# InventoryManagement
Inventory Management Software College project (Using Flask)

for setup:
    Clone and then create a python virtual environment and install the dependencies using 

    pip install -r requirements.txt
